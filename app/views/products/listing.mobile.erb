<ul class="rounded">
  <h1>Listing</h1>
  <% @products.each do |product| %>
  <li class="arrow">
    <%= link_to product.title, line_items_path(:product_id => product), :action => 'create', :method => :post, :remote => true %>
  </li>
  <% end %>
  
</ul>
